import styled from 'styled-components';
import EmptyWrapper from './EmptyPirate.styles';

const Wrapper = styled(EmptyWrapper)`
  p {
    margin-bottom: 16px;
  }

  a {
    color: var(--black);
  }
`;

export default Wrapper;
